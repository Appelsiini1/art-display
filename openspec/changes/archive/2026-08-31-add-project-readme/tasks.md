# Tasks: Implement project README

1. - [x] Create `README.md` at repository root with the canonical sections described in design.
   - Add badges (build, license) if CI available; otherwise omit.
   - Include an Architecture diagram (ASCII or small Mermaid snippet).

2. - [x] Add `docs/` directory and `docs/README.md` that links to existing component READMEs and notes where documentation is missing.

3. - [x] Link `kiosk/README.md` from the root README and add a short pointer to  `public/` docs.

4. - [x] After merge, optionally update other component docs incrementally (follow-up tasks).

Estimated effort: 1–2 hours to draft and open PR; follow-ups may require more time per-component.
